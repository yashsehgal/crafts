import { cn } from "../scripts/cn";
import { motion } from "framer-motion";

export default function CalendarSlotPicker({ time, bookedMeetings }) {
  return (
    <div className="calendar-slot-picker-component-container">
      <h1 className="leading-snug text-3xl font-medium tracking-tighter">
        {"a calendar slot picker."}
      </h1>
      <div className="calendar-slot-picker-component mt-16">
        
      </div>
    </div>
  )
}